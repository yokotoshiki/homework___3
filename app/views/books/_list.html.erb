<h2>New book</h2>
     <!--form_for部分-->
     <%= form_for(book) do |f| %>
 	       <% if book.errors.any? %>
 	           <div id="error_explanation">
 	               <h2><%= pluralize(book.errors.count, "book") %>
 	               prohibied this error from being saved: </h2>

 	               <% @book.errors.full_messages.each do |message| %>
                     <li><%= message %></li>
                 <% end %>
             </div>
         <% end %>

         <div class="field">
             <%= f.label :title %>
             <%= f.text_field :title, class: "form-control" %>
         </div>

         <div class="field" >
             <%= f.label :Opinion %>
             <%= f.text_area :body, class: "form-control" %></br>
         </div>

    　　　<div class="action">
             <button type="submit" class="btn btn-primary btn-md btn-block"> Create Book</button>
        </div>
    <% end %>